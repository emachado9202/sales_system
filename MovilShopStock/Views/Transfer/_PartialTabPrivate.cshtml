<div class="pull-right btn-editions">
    @if (RoleManager.IsInRole(RoleManager.Dealer) || RoleManager.IsInRole(RoleManager.Editor) || RoleManager.IsInRole(RoleManager.Administrator))
    {
        <a class="btn btn-sm btn-success ml-1" href="@Url.Action("CreatePrivate")">Crear Transferencia</a>
    }
</div>
<table id="data-table-private" class="table table-striped table-bordered" width="100%">
    <thead>
        <tr>
            <th class="no-sort">
            </th>
            <th>
                Hacia / Desde
            </th>
            <th>
                Monto
            </th>
            <th>
                Fecha
            </th>
        </tr>
    </thead>
    <tbody></tbody>
</table>